<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ecommerce.repository.UserRepository">

    <resultMap id="userResultMap" type="com.example.ecommerce.model.User">
        <result property="id" column="ID"/>
        <result property="name" column="NAME"/>
        <result property="email" column="EMAIL"/>
        <result property="username" column="USERNAME"/>
        <result property="password" column="PASSWORD"/>
    </resultMap>

    <insert id="insertUser" >
        INSERT INTO user (NAME, EMAIL, USERNAME, PASSWORD)
        VALUES (#{name}, #{email}, #{username}, #{password});
    </insert>

    <update id="updateUser">
        UPDATE user
        SET NAME     = #{name},
            EMAIL    = #{email},
            USERNAME = #{username},
            PASSWORD = #{password}
        WHERE ID = #{id};
    </update>

    <delete id="deleteUserById">
        DELETE
        from user
        WHERE ID = #{id};
    </delete>

    <select id="selectAllUser" resultMap="userResultMap">
        SELECT *
        FROM user
    </select>

    <select id="selectUserById" resultMap="userResultMap">
        SELECT *
        FROM user
        WHERE ID = #{id};
    </select>

<!--    Nếu tồn tại => true-->
    <select id="isExistsByName" resultMap="userResultMap">
        SELECT
            exists(select 1 from user where name="")
    </select>

</mapper>